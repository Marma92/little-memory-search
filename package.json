{
  "name": "back_confirmed",
  "version": "1.0.0",
  "description": "The problem consists in extracting data from a JSON file too big to be parsed / hold in memory as a whole.",
  "main": "index.js",
  "scripts": {
    "start": "node --max_old_space_size=50 index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marma92",
  "license": "ISC",
  "dependencies": {
    "JSONStream": "^1.3.5",
    "es": "^0.8.0",
    "event-stream": "^4.0.1"
  }
}
